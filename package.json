{
  "name": "nonsense-eradicator",
  "packageManager": "yarn@1.22.22",
  "version": "0.0.1",
  "scripts": {
    "dist:remove": "rm -rf dist",
    "dist:copy:html": "cp src/*.html dist/",
    "dist:copy:css": "cp src/css/*.css dist/",
    "build": "yarn dist:remove && tsc && yarn dist:copy:html && yarn dist:copy:css"
  },
  "dependencies": {
    "@types/chrome": "^0.0.277",
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}
